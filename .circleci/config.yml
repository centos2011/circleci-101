version: 2
jobs:
  one:
    docker: # See https://docs.docker.com/get-started/#docker-concepts if you are new to Docker.
      - image: node:9.6.1
    steps:
      - checkout
      - run: |
          echo "test"
workflows:
  version: 2
  one_and_two:
    jobs:
      - one
